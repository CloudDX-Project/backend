# 일정 종료 시각 및 긴 공백 보정

## 변경 목적

- 숙소 체크인 가능 시각(예: 15:00)을 첫날 일정 종료 시각으로 사용하지 않는다.
- 첫날은 관광·카페·저녁 식사를 마친 뒤 숙소로 이동한다.
- 숙소는 `CHECK_IN` 중간 방문 카드가 아니라 하루 마지막 `NIGHT_RETURN` 카드 한 건만 유지한다.
- 일정 사이에 90분 이상 공백이 생기면 전체 후보 풀의 미사용 관광지 또는 카페를 검토한다.
- 숙소 도착은 21:00을 상한으로 사용하며, 저녁 이후에도 가능한 일정이 있으면 추가한다.

## 처리 순서

1. Bedrock이 후보 장소와 기본 순서를 선택한다.
2. 서버가 중간 날짜의 식사 슬롯을 보정한다.
3. 첫날 저녁 식사가 없으면 17:30 이후 가능한 저녁 식당을 추가한다.
4. Kakao Mobility 실제 이동시간으로 전체 시간을 다시 계산한다.
5. 90분 이상 공백에는 다음 일정까지 도착 가능한 미사용 관광지·카페를 한 곳 추가한다.
6. 60~90분 일정이 들어가지 않으면 45분짜리 짧은 관광·카페 일정을 다시 검토한다.
7. 추가 장소도 불가능하면 18:30 고정 저녁을 버리고 17시 이후 가능한 시각으로 앞당긴다.
8. 21:00 숙소 도착 상한 안에서 저녁 이후 가능한 관광·카페도 검토한다.
9. 후보 삽입이 끝나면 21:00 anchor를 해제하고 실제 이동시간으로 숙소 도착 시각을 확정한다.

## 외부 API 호출 제한

- 이미 일정에서 사용한 장소는 다시 검토하지 않는다.
- 관광지와 카페는 점수 상위 후보 각각 최대 6개만 실제 경로로 검증한다.
- `RoutingService`의 동일 좌표 판정과 Redis 경로 캐시를 그대로 사용한다.

## 로컬 확인

API와 Worker가 같은 DB와 SQS Queue를 사용해야 한다. 기존 완료 일정은 자동으로 다시 생성되지 않으므로 새 일정 생성 요청으로 확인한다.

```powershell
.\gradlew.bat test --tests "com.travel.trip.plan.service.TripPlanSchedulePostProcessorTest"
```

예상 첫날 흐름:

```text
공항 도착 → 렌터카 → 점심 → 관광/카페 → 저녁 → 숙소 이동
```

중간 날짜에는 직전 일정 종료부터 다음 일정 시작까지 90분 이상 비는 경우, 실제 왕복 이동시간과 체류시간이 구간 안에 들어가는 장소만 추가한다.
